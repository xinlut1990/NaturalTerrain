#version 330



in vec4 position;
//in vec4 color;
in float id;

uniform mat4 projMatrix1;
uniform mat4 viewMatrix1;


out vec4 Color;

void main() 
{
	int idTemp = int(id);
	mat4 mat = projMatrix1;

	float b = (idTemp % 4 + 1) * 0.25;
	idTemp = idTemp >> 2;
	float g = (idTemp % 4  + 1) * 0.25;

   	Color = vec4(0.0,g,b,1.0);
    	gl_Position =  projMatrix1 * viewMatrix1 * position;
}